<% pages = Array.new %>
<% @pages.each do	|p| %>
	<% ns = p.page_namespace == 14 ? 'category' : 'page' %>
	<% pages.push({ 'page-title' => p.page_title.gsub('_', ' '), :ns => ns }) %>
<% end %>
<%= raw(pages.to_xml) %>